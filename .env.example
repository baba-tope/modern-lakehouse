# =============================================================================
# Texas Gas Lakehouse - Environment Configuration Template
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update all values with your actual configuration
# 3. NEVER commit .env to version control (it's in .gitignore)
# 4. Use this template as documentation for required variables
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
POSTGRES_USER=your_db_username
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=your_database_name
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# MinIO Object Storage (S3-Compatible)
# -----------------------------------------------------------------------------
MINIO_ROOT_USER=your_minio_admin
MINIO_ROOT_PASSWORD=your_minio_password_here

# S3 Region (for S3-compatible storage like MinIO)
# Use us-east-1 for MinIO compatibility
S3_REGION=us-east-1

# -----------------------------------------------------------------------------
# Airflow Configuration
# -----------------------------------------------------------------------------
# Generate Fernet key: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW_FERNET_KEY=your_fernet_key_here

# Generate secret key: openssl rand -hex 32
AIRFLOW_SECRET_KEY=your_secret_key_here

# Airflow Web UI Credentials
AIRFLOW_USERNAME=your_airflow_admin_username
AIRFLOW_PASSWORD=your_secure_airflow_password
AIRFLOW_FIRSTNAME=Your_First_Name
AIRFLOW_LASTNAME=Your_Last_Name
AIRFLOW_EMAIL=admin@yourcompany.com

# -----------------------------------------------------------------------------
# Grafana Configuration
# -----------------------------------------------------------------------------
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=your_grafana_password_here
GF_INSTALL_PLUGINS=
GF_SERVER_ROOT_URL=http://your_grafana_ip_here:30008
GF_ANALYTICS_REPORTING_ENABLED=false
GF_ANALYTICS_CHECK_FOR_UPDATES=false

# -----------------------------------------------------------------------------
# Kubernetes Configuration
# -----------------------------------------------------------------------------
NAMESPACE=texas-gas-lakehouse

# -----------------------------------------------------------------------------
# MetalLB IP Configuration
# -----------------------------------------------------------------------------
# Adjust this range based on your kind cluster's network
# To find available IPs: docker network inspect kind
METALLB_IP_RANGE=172.18.0.200-172.18.0.250

# Individual Service IPs (must be within METALLB_IP_RANGE)
# Nginx Ingress Controller gets priority IP for localhost access
INGRESS_IP=172.18.0.200
DASHBOARD_IP=172.18.0.209
DASHBOARD_API_IP=172.18.0.210
POSTGRES_IP=172.18.0.201
MINIO_IP=172.18.0.202
NESSIE_IP=172.18.0.203
TRINO_IP=172.18.0.204
DBT_IP=172.18.0.205
AIRFLOW_IP=172.18.0.206
PROMETHEUS_IP=172.18.0.207
GRAFANA_IP=172.18.0.208

# Data Generation Configuration
DATA_SCALING_FACTOR=50

# =============================================================================
# SECURITY NOTES:
# =============================================================================
# - All manifests use ${VARIABLE} placeholders that are replaced at deploy time
# - configure-ips.sh reads this file and updates K8s manifests
# - generate-dashboard.sh reads this file and updates dashboard HTML
# - create-secrets.sh reads this file and creates K8s secrets
# - Keep this .env file secure and never commit it to git
# =============================================================================
